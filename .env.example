# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=your_secure_password_here
DB_NAME=tyhh

# JWT Configuration
JWT_SECRET=generate_random_secret_key_here
JWT_EXPIRES_IN=86400
REFRESH_TOKEN_EXPIRES_IN=2592000
TOKEN_TYPE=Bearer

# Mail Configuration
MAIL_SERVICE=gmail
MAIL_AUTH_USER=your-email@gmail.com
MAIL_AUTH_PASS=your-app-password
MAIL_SENDER_FROM=TYHH
MAIL_JWT_SECRET=generate_mail_jwt_secret_here
MAIL_JWT_EXPIRES_IN=86400
MAIL_TOKEN_TYPE=Bearer

# Application URLs
CLIENT_URL=http://localhost:5173
ADMIN_URL=http://localhost:3000
BASE_URL=http://localhost:3002

# Pusher Configuration (Optional - for real-time features)
PUSHER_APPID=1
PUSHER_KEY=app1
PUSHER_SECRET=app1
PUSHER_CLUSTER=ap1
PUSHER_HOST=localhost
PUSHER_PORT=6001

# Redis Configuration
REDIS_REQUIRE=true
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# OpenAI API (Optional - if using AI features)
OPENAI_API_KEY=

# Google OAuth (Optional)
GOOGLE_CLIENT_ID=

# ImageKit Configuration (Optional - for image hosting)
IK_URL_ENDPOINT=
IK_PUBLIC_KEY=
IK_PRIVATE_KEY=
